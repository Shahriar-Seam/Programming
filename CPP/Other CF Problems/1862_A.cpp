#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t, n, m, i;
    string *s;
    string str = "vika";

    cin >> t;

    while (t--) {
        cin >> n >> m;

        s = new string[n]();

        for (i = 0; i < n; i++) {
            cin >> s[i];
        }

        
    }

    return 0;
}