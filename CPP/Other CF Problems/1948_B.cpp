#include <bits/stdc++.h>

#define yes puts("YES")
#define no puts("NO")

using namespace std;

void solve()
{
    int n, i, temp;
    vector <int> v, vv;

    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> temp;

        v.push_back(temp);
    }

    for (i = n - 1; i > 0; i--) {
        
    }

    yes;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;

    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}