#include <stdio.h>
#include "seam.h"

int main()
{
    int r, n;
    int a, b, c;
    char str[100];

    // printf("Enter the radius of the circle: ");
    // scanf("%d", &r);

    // printf("Enter the sides of the triangle: ");
    // scanf("%d %d %d", &a, &b, &c);

    // printf("Area of the circle = %g\n", area_of_circle(r));
    // printf("Area of the triangle = %g\n", area_of_triangle(a, b, c));

    // scanf("%s", str);
    // printf("%d\n", sum_of_digits_string(str));

    scanf("%d", &n);

    if (is_prime(n)) yes;
    else no;

    return 0;
}