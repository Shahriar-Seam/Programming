#include <bits/stdc++.h>

using namespace std;

struct segment_tree {
    
    void init(int n) {

    }
};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n, m;
    segment_tree st;

    cin >> n >> m;

    st.init(n);

    return 0;
}