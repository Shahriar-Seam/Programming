#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t, n, i, cnt;
    string s;

    cin >> t;

    while (t--) {
        cin >> n;
        cin >> s;

        while ()
    }

    return 0;
}