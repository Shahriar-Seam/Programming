#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t, n, i, cnt;
    string s;

    cin >> t;

    while (t--) {
        cin >> n >> s;

        cnt = 0;

        for (i = 0; i < len - 1; i++) {
            if (i && s[i] != s[i + 1]) {
                
            }
        }
    }

    return 0;
}